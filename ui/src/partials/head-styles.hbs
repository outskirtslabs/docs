    <script>
!function (theme, navWidth) {
  var dark = theme === 'dark' || (theme !== 'light' && matchMedia('(prefers-color-scheme: dark)').matches)
  if (dark) document.documentElement.classList.add('dark-theme')
  if (navWidth) document.documentElement.style.setProperty('--nav-width', `${navWidth}px`)
}(localStorage && localStorage.getItem('theme'),
  localStorage && localStorage.getItem('nav-width'))
    </script>
    <link rel="stylesheet" href="{{{uiRootPath}}}/css/site.css">
    <link rel="stylesheet" href="{{{uiRootPath}}}/css/vendor/onetrust.css">
    {{#if (asciidocExtensionRegistered '@asciidoctor/tabs')}}
    <link rel="stylesheet" href="{{{uiRootPath}}}/css/vendor/asciidoctor-tabs.css">
    {{/if}}
    {{#if env.SITE_ASCIINEMA_PROVIDER}}
    {{> asciinema-styles}}
    {{/if}}
